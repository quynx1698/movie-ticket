h1 Movies

a(href="/movies/create") New

form(action="/movies/search", method="GET")
    input(type="text", name="q")
    button Search

ul
    each movie in movies
        li
            span= movie.name
            span &nbsp
            a(href="/movies/" + movie.id) View